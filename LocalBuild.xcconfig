// LocalBuild.xcconfig
// Build configuration for local builds with an ad-hoc fallback when no Apple Development identity is available.
// Used by `make local` — does NOT affect Debug or Release configurations.

// Signing identity is injected by `make local` as LOCAL_CODE_SIGN_IDENTITY (an Apple
// Development certificate hash when one is available, otherwise "-" for ad-hoc).
// It must be threaded through a separate variable rather than overriding
// CODE_SIGN_IDENTITY directly on the command line: a command-line -xcconfig file
// outranks command-line build settings, so a hardcoded value here would always win.
CODE_SIGN_IDENTITY = $(LOCAL_CODE_SIGN_IDENTITY)
CODE_SIGN_STYLE = Manual
DEVELOPMENT_TEAM =
PROVISIONING_PROFILE_SPECIFIER =

// Entitlements are set via absolute path in the Makefile command line
// (relative paths break SPM package targets that resolve from their own source root)

// Add LOCAL_BUILD compilation flag so Swift code can use #if LOCAL_BUILD
SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) LOCAL_BUILD
