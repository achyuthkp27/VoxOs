# Domain docs

Before exploring, read `CONTEXT.md` at the repo root and any ADR in `docs/adr/` that touches the
area you are about to change. Use the vocabulary defined in `CONTEXT.md`. If your change
contradicts an ADR, say so explicitly rather than silently overriding it.
